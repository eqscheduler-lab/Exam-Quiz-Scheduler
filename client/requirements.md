## Packages
date-fns | Essential for calendar and schedule date manipulation

## Notes
Role-based access control is implemented client-side based on the `user.role` field.
The Schedule Grid handles logic for bell schedules (Periods 1-8 Mon-Thu, 1-4 Fri).
PDF generation is handled by the backend endpoint `/api/schedule/pdf`.
